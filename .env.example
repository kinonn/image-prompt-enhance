# No env vars required — providers are configured in UI (localStorage + proxy)
# Optional: set a default Zen base URL override (otherwise https://api.opencode.ai/v1)
# NEXT_PUBLIC_DEFAULT_ZEN_URL=https://api.opencode.ai/v1
# PORT=3000
