services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    # Optional: mount for debugging
    # volumes:
    #   - ./public:/app/public:ro
